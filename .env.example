# Copy to .env (gitignored) and fill in.

# Pinata API key for `pnpm deploy:pin` — create at
# https://app.pinata.cloud/developers/api-keys
# Scopes: pinFileToIPFS (required), pinList + unpin (optional, enables
# automatic pruning of superseded pins).
PINATA_JWT=
