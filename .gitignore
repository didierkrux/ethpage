node_modules
dist
.env
.env.*
!.env.example
.DS_Store

# Personal deployment configs, e.g. src/config.yourname.json
# (src/config.json stays the generic template). Remove these lines if you
# prefer to commit yours.
src/config.*.json
!src/config.json

# Local working docs (spec + implementation plan)
docs/superpowers/
